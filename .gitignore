# Environment variables — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/
backend/node_modules/

# Vite build output
dist/

# OS files
.DS_Store

# Runtime data — user picks/notes, never commit
backend/data/picks.json
backend/data/notes.json
# users.json intentionally NOT gitignored — contains only bcrypt hashes (safe to store)

# Lock files (generated, not needed in repo)
package-lock.json
backend/package-lock.json
